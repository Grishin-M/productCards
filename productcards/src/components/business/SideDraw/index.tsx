import React, { useState } from "react";
import { Drawer } from "@mui/material";

const SideDrawCustom = () => {
  const [open, setOpen] = useState<boolean>(false);
  return <div>index</div>;
};

export default SideDrawCustom;
